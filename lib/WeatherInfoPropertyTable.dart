import 'package:flutter/material.dart';

class WeatherInfoPropertyTable extends StatelessWidget {
  final Map<String, String> weatherInfoData;

  const WeatherInfoPropertyTable(this.weatherInfoData);

  @override
  Widget build(BuildContext context) {
    return Table(children: []);
  }
}
